"""Legacy compatibility command for commit-derived package versioning."""

print(
    "Package versions are generated from Git during builds by setuptools-scm; "
    "no source version file needs updating."
)
